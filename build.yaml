---
name: "AETHER Analysis"
guid: "ea32f6f5-de62-4784-9b60-4cfe664995ed"
version: "0.2.0.0"
targetAbi: "10.11.11.0"
framework: "net9.0"
overview: "Persistent multi-resolution AETHER video analysis storage"
description: >
  Stores compact feature timelines for AETHER clients, associates them with exact Jellyfin media
  sources and serves device-appropriate compact, balanced or full representations.
category: "General"
owner: "Bugstehude"
artifacts:
  - "Jellyfin.Plugin.AetherAnalysis.dll"
  - "aether-analysis-worker.cjs"
changelog: "0.2.0.0: in-plugin server-side analysis. The plugin now runs the shared AETHER perception-engine (visual + audio) on the server via a bundled Node worker and stores results directly — no upload/auth round-trip. Adds a scheduled task, an after-scan hook, and an analyze endpoint for the AETHER 'Server-Analyse' button, plus settings for Node path, sampling and libraries."
